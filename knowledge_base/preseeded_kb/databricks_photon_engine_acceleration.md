# Databricks Photon Query Engine Acceleration
Enable Photon runtime to speed up SQL and DataFrame API queries.

- **Photon** is a vectorized query engine written in C++ that integrates directly with Spark.
- Turn it on in the Databricks Cluster settings by enabling "Use Photon Acceleration".
