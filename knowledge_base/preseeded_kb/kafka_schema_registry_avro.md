# Enforcing Schemas in Kafka using Avro
Register schemas to maintain message contracts.

- Use **Confluent Schema Registry**.
- Define schemas in **Avro JSON** format.
- Set compatibility type to `BACKWARD`.
