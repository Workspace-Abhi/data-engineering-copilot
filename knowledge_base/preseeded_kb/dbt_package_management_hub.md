# Managing Package Dependencies in dbt
Add utility functions from the dbt Hub (like `dbt_utils` or `codegen`).

```yaml
# packages.yml
packages:
  - package: dbt-labs/dbt_utils
    version: 1.1.1
```
