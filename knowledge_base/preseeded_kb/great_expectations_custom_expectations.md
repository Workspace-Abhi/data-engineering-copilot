# Creating Custom Expectations in Great Expectations
Extend validation rules to check custom formats like email domain suffix or UUID integrity.

```python
class ExpectColumnToHaveValidDomain(MapMetricProvider):
    # custom expectation code
    pass
```
