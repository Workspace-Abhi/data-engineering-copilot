# Index Tuning and Execution Plans in SQL Server
Optimize queries by inspecting actual execution plans.

- Look for **Clustered Index Scan** (bad, reads full table).
- Look for **Index Seek** (good, reads specific keys).
